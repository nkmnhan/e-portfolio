{
  "name": "@eportfolio/root",
  "private": true,
  "scripts": {
    "dev:artist": "pnpm --filter artist-portfolio dev",
    "dev:byte": "pnpm --filter byte-folio dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
